# Server Port
PORT=3000

# MongoDB connection string
MONGO_URI=mongodb://localhost:27017/dynamic_proxy

# Admin Dashboard Key (used in dashboard.js and admin routes)
ADMIN_KEY=supersecretadminkey

# Optional: Default per-user limits (override in database if needed)
DEFAULT_REQUEST_LIMIT=1000
DEFAULT_STORAGE_LIMIT=104857600